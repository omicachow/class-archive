# Test strategy

Automated tests are added alongside each phase. Final acceptance uses the real HumHub application, MariaDB, installed Marketplace modules, queue/scheduler, and browser-visible behavior; mocks are not accepted as the final gate.

The required 32 authorization and lifecycle scenarios are tracked in `docs/acceptance-tests.md`.
